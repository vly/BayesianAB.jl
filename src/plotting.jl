# core plotting functions

plotbeta(α, β)
plotbeta() = plotBeta(1, 1)

plotunif(a, b)
plotunif() = plotUnif(0.0, 1.0)

plotbinom(n, p)
plotbinom() = plotBinom(1, 0.5)

plotbernoulli(p)
plotbernoulli() = plotBernolli(0.5)

plottest(t)
